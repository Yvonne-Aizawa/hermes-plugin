# Local source assets

This directory is for private/local avatar source assets. Binary avatar assets are ignored by git so the public repository can remain code-only.

Common local files:

```text
assets/lumina.vrm
assets/animations/vrma/*.vrma
assets/animations/*.fbx
```

If you publish a fork with assets included, verify the model and animation redistribution license first.
